
#!/bin/bash

# This is a script to send correlate predicted awareness with correctness e2 (pr).py as a batch job.
# it works on dataset pr

#$ -cwd
#$ -o test_run/out_pr.txt
#$ -e test_run/err_pr.txt
#$ -m be
#$ -q fsl.q
#$ -M nmei@bcbl.eu
#$ -N "e2_pr"
#$ -S /bin/bash

module load rocks-python-2.7

python "correlate predicted awareness with correctness e2 (pr).py"
